import { expect } from "chai";
import { loadFixture } from "@nomicfoundation/hardhat-network-helpers";
import { protocolFixture } from "../test";

describe("Fees", async function () {

    describe("When oracle is positive", async () => {
        describe("When users redeem shares", async () => {

        })

        describe("When oracleError increases", async () => {

        })

        describe("When oracleError resets", async () => {

        })
    })

    describe("When oracle is negative", async () => {
        describe("When users redeem shares", async () => {

        })

        describe("When oracleError increases", async () => {

        })

        describe("When oracleError resets", async () => {

        })
    })

})